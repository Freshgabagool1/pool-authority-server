{
  "name": "pool-authority-server",
  "version": "1.1.0",
  "description": "Pool Authority - Stripe Payments + Resend Email Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "stripe": "^14.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```

7. **Click "Commit changes"**

---

Render will auto-redeploy. Wait 1-2 minutes, then check the logs - you should see:
```
Email: ✅ Configured (Resend)
